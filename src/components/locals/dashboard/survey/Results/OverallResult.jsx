import { Box } from '@mui/material';
import { useStyles } from './styles';

const OverallResult = () => {
  const classes = useStyles();
  return <Box className={classes.overallRoot}>OverallResult</Box>;
};

export default OverallResult;
