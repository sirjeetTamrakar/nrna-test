import { LinearProgress } from "@mui/material";
import React from "react";

const CustomLoader = () => {
  return (
    <div>
      <LinearProgress />
    </div>
  );
};

export default CustomLoader;
