import Candidate from 'components/locals/NCC/NccSite/Candidate';

const CandidatePage = () => {
  // useEffect(() => {
  //   dispatch(getSiteSettings({ type: 'ncc', id: user?.id }));
  // }, []);
  return <Candidate title="Who we are" />;
};

export default CandidatePage;
